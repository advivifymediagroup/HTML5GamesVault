# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.vscode/
.idea/

# Server-only deploy secret/log — created once directly on the server,
# never committed or pulled.
deploy-secret.php
deploy.log
